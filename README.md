# shrinkr-url-shortener

Fast, secure URL shortener with analytics and custom aliases.

## Getting Started

```powershell
npm install
npm run dev
```

Open http://localhost:3000. See `ARCHITECTURE.md`, `API.md`, and `DEPLOYMENT.md` for project details.
